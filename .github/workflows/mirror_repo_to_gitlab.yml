name: Mirror repo to GitLab

on: [push, delete]

jobs:
  call-nss-ops-mirror-workflow:
    uses: ITMO-NSS-team/NSS-Ops/.github/workflows/mirror-repo.yml@c90a2219cd8cd112cbbe893b87d7c4b1db905610
    with:
      GITLAB_URL: 'https://gitlab.actcognitive.org/itmo-nss-team/FEDOT.git'
    secrets:
      GITLAB_USER: ${{ secrets.GITLAB_USER }}
      GITLAB_PASSWORD: ${{ secrets.GITLAB_PASSWORD }}
      


