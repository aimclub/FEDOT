# Package Hotspots

## `fedot/api`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- planner extraction
- preset or assumption modeling
- recommendation rules
- facade compatibility

## `fedot/core`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- pipeline planning
- operation normalization
- repository queries
- execution or evaluation rules

## `fedot/preprocessing`

Often routes to:

- `$fedot-pure-core-shell`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- merge planning
- source resolution
- optional preprocessing decisions
- encoding or orientation choices

## `fedot/remote`

Often routes to:

- `$fedot-safe-configs`
- `$fedot-typed-domain-errors`
- `$fedot-invariant-tests-review`

Clues:

- remote mode and config parsing
- client setup
- dataset or execution parameter normalization

## `fedot/extensions`

Often routes to:

- `$fedot-extension-contract`
- `$fedot-invariant-tests-review`
- `$fedot-typed-domain-errors`

Clues:

- manifest design
- registry updates
- runtime rules
- parameter rules
- operation discovery