{
  "metadata": {
    "regr": {
      "accepted_node_types": ["any"],
      "description": "Implementations of the regression models",
      "forbidden_node_types": "[]",
      "input_type": "[DataTypesEnum.table]",
      "output_type": "[DataTypesEnum.table]",
      "tags": ["ml", "atomized", "non-default"],
      "tasks": "[TaskTypesEnum.regression, TaskTypesEnum.ts_forecasting]",
      "strategies": ["fedot.core.operations.evaluation.atomized", "FedotAtomizedStrategy"]
    }
  },
  "operations": {
    "atomized_ts_differ": {
      "meta": "regr",
      "presets": ["fast_train", "ts"],
      "tags": ["linear"]
    },
    "atomized_ts_sampler": {
      "meta": "regr",
      "presets": ["fast_train", "ts"],
      "tags": ["linear", "not-for-mutation"]
    },
    "atomized_ts_scaler": {
      "meta": "regr",
      "presets": ["fast_train", "ts"],
      "tags": ["linear", "not-for-mutation"]
    },
    "atomized_ts_to_time": {
      "meta": "regr",
      "presets": ["fast_train", "ts"]
    }
  }
}