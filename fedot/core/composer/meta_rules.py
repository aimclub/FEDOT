from typing import Dict

from fedot.core.data.data import InputData


def get_cv_folds_number(input_data: InputData) -> Dict[str, int]:
    pass


# what if no exact preset is needed, how to maintain default?
def get_recommended_preset(input_data: InputData) -> Dict[str, int]:
    pass
